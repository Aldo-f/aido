#!/bin/bash
# Pre-push hook to prevent direct pushes to main

protected_branch="main"

while read local_ref local_sha remote_ref remote_sha; do
    if [[ "$remote_ref" == "refs/heads/$protected_branch" ]]; then
        echo "❌ ERROR: Direct push to '$protected_branch' is not allowed."
        echo "   Push to 'develop' instead, then create a PR."
        echo ""
        echo "   To push to develop:"
        echo "     git push origin your-branch:develop"
        exit 1
    fi
done
